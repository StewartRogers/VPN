*.log
*.log.*.gz
*.ovpn
__pycache__/
*.pyc

# VPN runtime logs and state
vpn_logs/

# qBittorrent runtime state (not a template — regenerated at runtime)
qBittorrent-data.conf

# Backup and temporary files (outside repo, listed for reference only)
# ~/.vpn_backups/ — iptables/DNS backups, never in the repo

# Secrets
.env
.env.*
!.env.example

# Diagnostic output — TROUBLESHOOTING.md tells you to generate this in the repo
# and it contains `ip a` output, i.e. your real IP addresses.
diagnostic.txt
pipout.txt

# Python virtualenvs
.venv/
venv/

# Runtime state
*.pid
nohup.out

# Editor / merge leftovers
*.bak
*.orig
*.rej

# IDE and system files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# qBittorrent logs
qbit.log
checkvpn.log
